this folder contains programs for detecting clustered interferers in non-contiguous bands

but right now we still need to detect interferers in contiguous ~100 MHz band rather than in non-contiguous one